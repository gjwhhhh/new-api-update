# Generate with: openssl rand -base64 32
CONVERSATION_AUDIT_ENABLED=true
CONVERSATION_AUDIT_RETENTION_DAYS=30
CONVERSATION_AUDIT_MAX_BYTES=262144
# Temporary JSON inspection limit only; the original body is never persisted by the audit extension.
CONVERSATION_AUDIT_MAX_PARSE_BYTES=4194304
CONVERSATION_AUDIT_ACTIVE_KEY_VERSION=v1
CONVERSATION_AUDIT_KEY_V1=
